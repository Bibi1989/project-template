# GitHub Actions discovers workflows only from the repository-root
# `.github/workflows/` directory. These files mirror the per-service
# definitions under frontend/ and backend/ so the monorepo deploys correctly.
